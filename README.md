# CRM Frontend

React приложение для управления клиентами (CRM) с использованием TypeScript, Vite, Redux Toolkit и React Router.

## 🚀 Быстрый старт

### Установка зависимостей
```bash
npm install
```

### Запуск в режиме разработки
```bash
npm run dev
```

### Сборка для production
```bash
npm run build
```

### Превью сборки
```bash
npm run preview
```

## 📦 Деплой на GitHub Pages

Проект настроен для автоматического деплоя на GitHub Pages. Деплой успешно выполнен!

### Ручной деплой
```bash
# Установите зависимости (используется pnpm)
pnpm install

# Соберите проект и задеплойте
pnpm run deploy
```

Или используйте npm:
```bash
npm run deploy
```

### Автоматический деплой через GitHub Actions
При пуше в ветки `main` или `master` автоматически запускается workflow, который:
1. Собирает проект
2. Деплоит на GitHub Pages

Также доступен ручной workflow для деплоя из других веток через GitHub Actions UI.

### Ссылка на деплой
После успешного деплоя приложение будет доступно по адресу:
`https://[ваш-username].github.io/crm-frontend/`

### 🔧 Устранение проблем

#### Ошибка "gh-pages не является внутренней или внешней командой"
Если возникает ошибка при выполнении `npm run deploy`:
1. Убедитесь, что зависимости установлены:
   ```bash
   pnpm install
   ```
   или
   ```bash
   npm install
   ```

2. Проверьте, что `gh-pages` добавлен в devDependencies в `package.json`

3. Используйте pnpm вместо npm (рекомендуется, так как проект использует pnpm-lock.yaml):
   ```bash
   pnpm run deploy
   ```

#### Проблемы с роутингом на GitHub Pages
Приложение автоматически переключается на `HashRouter` при деплое на GitHub Pages для избежания ошибок 404.

### 📊 Статус деплоя
![GitHub Pages](https://img.shields.io/badge/GitHub%20Pages-Deployed-success?style=flat-square)
![GitHub Actions](https://img.shields.io/badge/GitHub%20Actions-Configured-blue?style=flat-square)

## ⚙️ Конфигурация

### Настройки Vite
- Базовый путь: `/crm-frontend/` в production режиме
- Поддержка HashRouter для GitHub Pages
- Алиасы для удобного импорта (`@/`, `@components/`, и т.д.)

### Роутинг
Проект использует:
- `HashRouter` при деплое на GitHub Pages
- `BrowserRouter` в development режиме

## 🛠 Технологии

- **React 19** с TypeScript
- **Vite** для сборки
- **Redux Toolkit** для управления состоянием
- **React Router 7** для навигации
- **Sass/SCSS** для стилей
- **GitHub Pages** для хостинга
- **GitHub Actions** для CI/CD

## 📁 Структура проекта

```
src/
├── api/           # API клиент и конфигурация
├── assets/        # Статические ресурсы
├── components/    # Переиспользуемые компоненты
├── constants/     # Константы приложения
├── hooks/         # Кастомные React хуки
├── pages/         # Страницы приложения
├── router/        # Конфигурация роутинга
├── store/         # Redux store и слайсы
└── widgets/       # Виджеты (header, footer)
```

## 🔧 Расширение ESLint

Если вы разрабатываете production приложение, рекомендуется обновить конфигурацию ESLint:

```js
export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      tseslint.configs.recommendedTypeChecked,
      tseslint.configs.strictTypeChecked,
      tseslint.configs.stylisticTypeChecked,
    ],
    languageOptions: {
      parserOptions: {
        project: ['./tsconfig.node.json', './tsconfig.app.json'],
        tsconfigRootDir: import.meta.dirname,
      },
    },
  },
])
```

## 📄 Лицензия

MIT
