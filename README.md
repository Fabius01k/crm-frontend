# Schedule Frontend

React приложение для управления расписанием с использованием TypeScript, Vite, Redux Toolkit и React Router.

## 🚀 Быстрый старт

### Установка зависимостей
```bash
npm install
```

### Запуск в режиме разработки
```bash
npm run dev
```

### Сборка для production
```bash
npm run build
```

### Превью сборки
```bash
npm run preview
```

## 📦 Деплой на GitHub Pages

Проект настроен для автоматического деплоя на GitHub Pages.

### Ручной деплой
```bash
# Установите gh-pages глобально (если еще не установлено)
npm install -g gh-pages

# Соберите проект и задеплойте
npm run deploy
```

### Автоматический деплой через GitHub Actions
При пуше в ветки `main` или `master` автоматически запускается workflow, который:
1. Собирает проект
2. Деплоит на GitHub Pages

Ссылка на деплой: `https://[ваш-username].github.io/schedule-frontend/`

## ⚙️ Конфигурация

### Настройки Vite
- Базовый путь: `/schedule-frontend/` в production режиме
- Поддержка HashRouter для GitHub Pages
- Алиасы для удобного импорта (`@/`, `@components/`, и т.д.)

### Роутинг
Проект использует:
- `HashRouter` при деплое на GitHub Pages
- `BrowserRouter` в development режиме

## 🛠 Технологии

- **React 19** с TypeScript
- **Vite** для сборки
- **Redux Toolkit** для управления состоянием
- **React Router 7** для навигации
- **Sass/SCSS** для стилей
- **GitHub Pages** для хостинга
- **GitHub Actions** для CI/CD

## 📁 Структура проекта

```
src/
├── api/           # API клиент и конфигурация
├── assets/        # Статические ресурсы
├── components/    # Переиспользуемые компоненты
├── constants/     # Константы приложения
├── hooks/         # Кастомные React хуки
├── pages/         # Страницы приложения
├── router/        # Конфигурация роутинга
├── store/         # Redux store и слайсы
└── widgets/       # Виджеты (header, footer)
```

## 🔧 Расширение ESLint

Если вы разрабатываете production приложение, рекомендуется обновить конфигурацию ESLint:

```js
export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      tseslint.configs.recommendedTypeChecked,
      tseslint.configs.strictTypeChecked,
      tseslint.configs.stylisticTypeChecked,
    ],
    languageOptions: {
      parserOptions: {
        project: ['./tsconfig.node.json', './tsconfig.app.json'],
        tsconfigRootDir: import.meta.dirname,
      },
    },
  },
])
```

## 📄 Лицензия

MIT
